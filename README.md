# Smart Kirana Booking System

A complete booking system for Kirana stores.

## How to Run

1. Clone the repository
2. Run `npm install`
3. Create `.env` file and add your credentials
4. Run `npm start`
